kind = "web"
previewPath = "/"
title = "Smart Contract Auditor"
version = "1.0.0"
id = "artifacts/auditor"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24915

[services.development]
run = "pnpm --filter @workspace/auditor run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/auditor", "run", "build" ]
publicDir = "artifacts/auditor/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24915"
BASE_PATH = "/"
